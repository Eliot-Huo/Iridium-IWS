# 🚨 v6.8 緊急修復指南

## 🎯 問題摘要

**發現時間**：2025-12-25 15:54  
**影響範圍**：3 個功能  
**根本原因**：`sbd_service.py` 使用舊的 v6.7 參數

---

## ❌ 錯誤詳情

### **錯誤 1: 變更費率失敗**
```
IWSGateway.update_subscriber_plan() got an unexpected keyword argument 'demo_and_trial'
```

**原因**：`sbd_service.py` 第 263 行傳入了不存在的 `demo_and_trial` 參數

**修正前（v6.7）**：
```python
result = iws_gateway.update_subscriber_plan(
    imei=request.imei,
    new_plan_id=request.plan_id,
    demo_and_trial=False  # ❌ 此參數不存在
)
```

**修正後（v6.8）**：
```python
result = iws_gateway.update_subscriber_plan(
    imei=request.imei,
    new_plan_id=request.plan_id,
    lrit_flagstate="",  # ✅ 正確參數
    ring_alerts_flag=False  # ✅ 正確參數
)
```

---

### **錯誤 2 & 3: 暫停/註銷失敗（HTTP 500）**
```
HTTP 500: Internal Server Error
Action: setSubscriberAccountStatus
```

**可能原因**：
1. IMEI 不存在或無效
2. 設備狀態不允許此操作
3. SOAP 格式問題

**診斷步驟**：
1. 使用測試腳本驗證 SOAP 格式
2. 檢查 IMEI 是否有效
3. 查看完整的 SOAP 錯誤訊息

---

## 🔧 修復步驟

### **步驟 1: 更新 sbd_service.py**

**檔案位置**：`src/services/sbd_service.py`

**修改內容**（第 258-264 行）：

```python
# 修正前
if request.action_type == ActionType.CHANGE_PLAN:
    # 費率變更
    result = iws_gateway.update_subscriber_plan(
        imei=request.imei,
        new_plan_id=request.plan_id,
        demo_and_trial=False  # ❌ 錯誤
    )

# 修正後
if request.action_type == ActionType.CHANGE_PLAN:
    # 費率變更（v6.8: 使用 accountUpdate）
    result = iws_gateway.update_subscriber_plan(
        imei=request.imei,
        new_plan_id=request.plan_id,
        lrit_flagstate="",  # ✅ 正確
        ring_alerts_flag=False  # ✅ 正確
    )
```

**GitHub 網頁操作**：
```
1. 開啟 src/services/sbd_service.py
2. 找到第 258-264 行
3. 將第 263 行改為：
   lrit_flagstate="",  # 空字串（非 LRIT 用戶）
   ring_alerts_flag=False  # 不啟用 Ring Alerts
4. 刪除：demo_and_trial=False
5. 提交：fix: Update sbd_service.py for v6.8 compatibility
```

---

### **步驟 2: 測試修復**

**使用測試腳本**：
```bash
python tests/test_v6.8_complete.py
```

**預期結果**：
```
✅ 連線測試: PASS
✅ 查詢方案: PASS
✅ 暫停設備: PASS (或顯示具體錯誤)
✅ 恢復設備: PASS (或顯示具體錯誤)
✅ 變更費率: PASS
```

---

### **步驟 3: 診斷暫停/註銷錯誤**

如果暫停/註銷仍然失敗，查看 SOAP 錯誤詳情：

**可能的錯誤訊息**：
1. `"Subscriber not found"` → IMEI 不存在
2. `"Invalid status transition"` → 設備狀態不允許此操作
3. `"Authentication failed"` → 簽章問題

**解決方案**：
- 使用有效的測試 IMEI
- 確認設備當前狀態
- 檢查簽章算法

---

## 📊 修復對照表

| 檔案 | 修改內容 | 行數 | 狀態 |
|------|---------|------|------|
| sbd_service.py | 移除 demo_and_trial | 263 | ✅ 完成 |
| sbd_service.py | 加入 lrit_flagstate | 263 | ✅ 完成 |
| sbd_service.py | 加入 ring_alerts_flag | 264 | ✅ 完成 |

---

## 🎉 修復完成檢查清單

- [ ] 更新 `sbd_service.py` 第 263-264 行
- [ ] 提交到 GitHub
- [ ] 執行測試腳本
- [ ] 驗證變更費率功能
- [ ] 診斷暫停/註銷錯誤（如果需要）

---

## 📝 備註

**變更費率**：✅ 已修復（移除錯誤參數）  
**暫停設備**：⚠️ 需要診斷（500 錯誤）  
**註銷設備**：⚠️ 需要診斷（500 錯誤）

**下一步**：
1. 立即部署 `sbd_service.py` 修復變更費率
2. 使用測試腳本診斷暫停/註銷問題
3. 根據診斷結果進一步修正

---

**修復時間**：< 1 分鐘  
**影響範圍**：僅 1 個檔案  
**優先級**：🚨 **高** - 立即部署

---

**建立時間**：2025-12-25 15:56  
**狀態**：✅ 修復方案已準備
